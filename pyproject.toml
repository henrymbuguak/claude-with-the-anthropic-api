[project]
name = "claude-with-the-anthropic-api"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.2",
    "python-dotenv>=1.2.2",
]
